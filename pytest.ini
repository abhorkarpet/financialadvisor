[pytest]
testpaths = tests
addopts = -v --tb=short
asyncio_mode = strict
markers =
    e2e: end-to-end browser tests (require running Streamlit server)
    chat: requires OPENAI_API_KEY
    slow: takes >30s (Monte Carlo simulation)
